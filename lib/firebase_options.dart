// PLACEHOLDER — replace this file by running:
//
//   dart pub global activate flutterfire_cli
//   flutterfire configure
//
// That command generates the real `DefaultFirebaseOptions` for your Firebase
// project (Android + iOS). Until then `main.dart` catches the error below and
// starts the app in demo mode with sample data.

import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'firebase_options.dart is a placeholder. Run `flutterfire configure`.',
    );
  }
}
